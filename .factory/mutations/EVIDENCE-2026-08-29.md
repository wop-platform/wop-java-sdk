# 门灵敏度证据 — wop-java-sdk（S1 缺陷集全量重证）

- 日期：2026-08-29（移植安装当日首证；替代移植源 gtsp-wop-gateway 的 EVIDENCE-2026-08-24.md——缺陷集已按本仓文件重写，源仓证据对本仓不构成证明）
- 周界指纹（factory-local.json git blob）：`642c2b248381…`（evidence-stamp.json 绑定；改周界后须重跑 run.py 重证）
- 门：guard = `python3 .factory/guard.py --files <target>`；tests = `scripts/run_tests.sh --no-lock`（mvn -q verify，194 测试 + jacoco LINE/BRANCH 1.00 门禁，基线 rc=0 ≈ 8s）
- 行为类锚点来源：docs/mutation-report.md 手工变异报告（14/14 击杀）之代表性子集

## 结果（run.py 全量，13/13 按预期，无 SKIP/FAIL-config）

| id | 门 | 语义 | 判定 |
|---|---|---|---|
| G-01 | guard | MISSION 铁律篡改 | PASS（rc=1） |
| G-02 | guard | guard.py 配置载入改向（fail-closed） | PASS（rc=2） |
| G-03 | guard | run_tests.sh 去 pipefail | PASS（rc=1） |
| G-04 | guard | 测试类 javadoc 良性变更（负例） | PASS（rc=0 放行） |
| M-01 | guard | MISSION 周界单边加条目（self_check 漂移） | PASS（rc=2） |
| M-02 | guard | MISSION 周界指向不存在路径 | PASS（rc=2） |
| B-01 | tests | Transport javadoc 良性变更（负例） | PASS（rc=0 放行） |
| B-101 | tests | Codec.b64UrlDecode 长度校验失效 | PASS（rc=1） |
| B-102 | tests | DekPayload 三段校验反转 | PASS（rc=1） |
| B-103 | tests | WopClient digest 前置判定 &&→\|\| | PASS（rc=1） |
| B-104 | tests | ContentDigest.build 双空格 | PASS（rc=1） |
| B-105 | tests | EncryptHeader.isEncrypted 恒假 | PASS（rc=1） |
| B-106 | tests | CanonicalRequest 分隔符 \n→\r\n | PASS（rc=1） |

```
篡改类拦截（guard 门）: 5/5 = 100%
行为破坏类拦截（tests 门）: 6/6 = 100%
正向缺陷拦截（kill rate）: 11/11 = 100%
负例放行: 2/2
结论: 门灵敏度冒烟通过（auto-merge 的必要非充分条件）
```

附：`.factory/tests` pytest 243 passed（移植完整性：guard/factory_lib/hosting/state/mutations 消费本地化配置全部 fail-closed 语义正常）。

> 记录：run.py `write_stamp` 的 evidence 默认参数为源仓文件名（EVIDENCE-2026-08-24.md，
> 零本地化 full 分发件不作仓内改动）；stamp 的 evidence 字段已人工指向本文件——
> 该字段无程序消费方（run.py 仅比对 perimeter_blob），此为诚实性修正而非语义变更。
